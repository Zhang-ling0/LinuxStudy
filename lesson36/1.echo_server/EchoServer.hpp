# ifndef _ECHOSERVER_HPP
# define _ECHOSERVER_HPP
#include<iostream>
class UdpServer
{
    UdpServer(){};
    ~UdpServer(){};
    //网络实际上也只是结构体对象，创建好套接字返回值实际上就只是一个文件描述符
    void Init(){

    }
    void Start(){};

};

# endif _ECHOSERVER_HPP
